cache_dir = "${BPM}/../../bpm/cache"
database = "${BPM}/../../bpm/db"
use_default_target = true

[providers]
test = "http://localhost:8000/pkg"

[mount]
TARGET = "${BPM}/../../bpm/install"
TEST = "${BPM}/../../bpm/install/test"
